# 统一换行：仓库里一律 LF（跨平台协作/打包的最省事选择）
* text=auto eol=lf

# Windows 批处理与 PowerShell 保持 CRLF（否则某些老工具会出问题）
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制
*.png binary
*.jpg binary
*.ico binary
*.zip binary
*.gz binary
