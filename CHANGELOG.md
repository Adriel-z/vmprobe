# 更新日志

本项目遵循"每个里程碑一个版本"的粗粒度版本号：`0.x.0` 对应一个里程碑。
每个版本的**证据**（测试项数、真机验证结论）与**缺陷台账**分别见 `DEVELOPMENT.md` 与 `ISSUES.md`。

---

## v0.3.0 —— M3「归档与迁移」+ M5「审计链 HMAC」+ I5 决策（2026-09-15）

### 新增

- **归档与迁移（`.vmpz`，M3）**：把一台机器上的状态（目标定义、环境画像、日报、加载台账，
  可选审计链与运行记录）**完整搬到另一台**。格式是**标准 zip**（自研容器约 200 行、零依赖），
  能被 Windows 资源管理器与 `unzip` 直接打开 —— 测试里用 **PowerShell `Expand-Archive`**
  做外部实现交叉验证，避免"只有我自己能读"的私有格式蒙混过关。
  四条纪律：清单逐文件 sha256（不符即拒）、导入前差异预览（新增/一致/冲突/**凭据缺口**）、
  默认预演且默认不覆盖（覆盖先备份为 `.vmpz-bak`）、脱敏档位（`none`/`minimal`/`standard`）。
- **私钥迁移必须加密**：`--include-secrets` 用 scrypt + AES-256-GCM；
  口令只从 TTY 或环境变量给（**不接受命令行参数** —— 那会进 shell 历史与 `ps`），
  且"带秘密但不加密"这个组合在代码里被直接拒绝。
- **审计链 HMAC（M5 部分）**：默认自动生成密钥（`<storageDir>/audit-hmac.key`，0600），
  审计从"能检出改动"升级为"**伪造需要密钥**"。逐条记 `algo`/`keyId`（两者都进哈希正文），
  **禁止降级**（hmac 之后不得回到 sha256），**换密钥 ≠ 被篡改**（报 `keyMismatchAt` 并说明原因）。
  旧的无密钥链**照旧可验**，不需要迁移。
- **运行日志 `logs/run.jsonl`**：与审计分工 —— 带 `level`、面向人看与 `grep`、**不入哈希链**
  （日志格式变更不该破坏证据链）；只留一代历史。

### 变更

- **I5 决策：`approval` 从必需注入改为可选**。原来缺审批服务时插件的 `apply()` 根本不会执行
  （工具一个都没注册），甚至会让那条 loader entry 失败、**整棵插件树加载不出来** ——
  一个可选服务的缺失不该有这种权力。现在：插件照常加载，**R0/R1 照常执行**，
  **R2/R3 一律拒绝**，并在日志 / 加载台账 / `vmprobe_status` 三处显示"审批服务不可用"。
  安全属性没有变弱（没有审批就绝不执行特权且不可逆的动作），验证方式加强为
  "断言 `transport.apply` **一次都没被调用**"。
  ⚠️ 这一条在发布时**有落地缺陷**（只改了"要不要注入"、没改"怎么读"），当天真机启动即失败；
  修正见下方「修复」第一条与 `ISSUES.md` §15。

### 修复

- **（第九轮 · 真机验证）I5 落地缺陷：读未注入的服务名会抛异常，整棵插件树起不来。**
  `approval` 从 `inject` 里拿掉之后，`apply()` 里那句 `ctx.approval && …` 自己就会崩：
  cordis 的 ctx 是 Proxy，读"已声明但本 fiber 未注入"的服务名**抛异常**
  （`cannot get property "approval" without inject`），不是返回 `undefined` ——
  而 `apply()` 抛错 = 那条 loader entry 失败 = **整棵树加载失败**（用户实测：`dsh web` 直接起不来）。
  现在所有可选服务一律经新增的 `plugin-host/src/services.js`（`ctx.get(name)`）读取，
  "加载期永不阻塞 + 执行期 fail-closed"的 I5 意图**现在才真的成立**。
  同源修掉的还有：`ctx.credentials`（凭据解析路径，尚未引爆）与
  `systemPrompt.section({id,title,content})` —— 该 API 要的是 `{name, order, text}`，
  三个字段全错、异常被 try/catch 吞成"宿主不支持"，**基础用法提示从未注入过**。
- **归档 schema 版本号解析取错段**：`schema.split('/')[1]` 取到的是 `archive` 而非版本号（→ `NaN`），
  于是"版本太新"被误报成"没有迁移路径" —— 结论虽然仍是否决，但**理由说错了**，
  而理由正是运维的判断依据（"去升级主控端" vs "格式不认识"）。
- **`maxRunLogBytes` 缺默认值 → 运行日志每次写入都轮转**（`size <= undefined` 恒为 false），
  症状是"运行日志永远只有一行"，极具迷惑性。已补默认值并加专门的回归测试。
- **（第十轮）`npm test` 在 Node 22.22.2 下跑不起来**：`node --test packages/core/test/ …`
  的目录参数在该版本**不被当作"在这些目录里找测试"**，而是被当成三个入口模块执行 →
  `Cannot find module '…\packages\core\test'`，`1..3 / pass 0 / fail 3`。
  于是 `npm run check` 的**第一层在这台机器上必然失败**，"全绿"只能靠人工绕过第一层。
  改用默认测试发现（`node --test`，收集到的 174 项与改造前同一集合，默认排除 `node_modules`）：
  实测 `npm test` → **174 / 174 通过**。

### 测试

单元测试 **143 → 174 项**；新增 `npm run check:archive`（12 项，含外部解压交叉验证）。
`npm run check` 现在有**八层**（单测 / 插件契约 / 故障推演 / SSH 端到端 / 归档 / 文档 / 预检 / 协从端），全绿。

**第九轮补强（插件契约检查）**：新增两类探测点 ——
① 模拟 cordis 代理语义（未注入的服务名**必须抛**），② 用 DSH 自带的**真实 cordis**
起一棵最小插件树（兄弟 fiber 提供 `approval`/`tools`，VMProbe 未注入它们）并断言加载成功。
之所以必须补：I5 提交时八层检查**全绿**，因为契约检查用的 ctx 是裸对象，
读不存在的字段只得到 `undefined`，正好把当时的错误假设"验证"了一遍（`ISSUES.md` §15.3）。

### 未做（明确说明，不假装）

- **M4 协从端守护进程 / 本地定时**：其价值全在 Linux 特有行为（unix socket / systemd / logrotate），
  本机无 Linux 目标 → 只能半验证。按"能验证才敢说已实现"的纪律**显式推迟**（`ISSUES.md` §13.5）。
- **客户端 UI 插件（浏览器侧）**：本机发行版无客户端打包器、客户端 peer 包未发布（`ISSUES.md` §12.4）。

---

## v0.2.0 —— M2「执行的可信度」（2026-09-14）

M1 让"能连上并执行"成立；M2 让"**真的做到了吗**"这个问题有答案。

### 新增

- **执行后校验（`verify`）真执行**：动作可声明
  `verify { probe, expect, maxWaitMs }`，apply 之后**真的再探测一次**并给出三态结论
  —— `true`（达到目标态）/ `false`（未达到）/ `null`（未判定：未声明 expect、探测未实现、预演）。
  "命令返回 0"与"达到目标态"**分开报**；`system.update` 现在能回答"还剩几个包没更新"。
- **运行记录落盘**：`runs/<runId>.log` 存单次执行的完整输出（计划摘要、逐条命令、退出码、
  stdout/stderr、校验结论），写入前过**与审计同一套**脱敏；默认上限 4 MiB（超出截断并标注）；
  审计只保留 `{ runPath, runSha256, runBytes, exit, verify }` 引用；`verifyRunSeal()` 可核对是否被改动。
- **取消真贯通**：`exec.signal` 一路传到 SSH 通道（先给远端 TERM，3 秒后硬关通道）。
  取消与失败**分开记账**（`action.cancelled`），取消关的是通道而非连接（实测中断后连接仍可用）。
  工具自此**声明 `timeoutMs`** —— 因为"与 `exec.signal` 协作"这句话现在是真的。
- **心跳保活与连接健康观测**：60s 一次、只探已有会话、失败即标 `detached`，
  并记住**最初失败原因**；状态工具显示延迟 / 连续失败次数 / 最初原因。
- **日报自动清理**：保留近 90 天 + 更早的每月 1 号，按 UTC 日键幂等（每天一次，重启自动补跑）。

### 修复

- **审批阈值不再是死键**：`autoAllowUpTo` 此前在决策函数里**完全没参与判定**（文档说生效、代码无效），
  插件配置也透传不进去。现在两处都真的接线，并把**实际生效的策略**在启动时写入审计（`config.effective`）。
- **配置问题三重可见**：未知键、非法值、比默认更宽松的策略 → 日志 + 加载台账 + `vmprobe_status`。
  `allowRawShell` 不再是"看起来存在的开关"（T3 未实现，写 `true` 会被明确拒绝）。
- **`makeTarget` 拒绝对象型 `authRef`**：此前会把引用嵌套成 `ref: { kind, ref }`，
  在宽松凭据解析器下**照样能连上**，直到更严格的消费者才以"凭据 `[object Object]` 尚未配置"暴露。
- **开发工具不再依赖本机绝对路径**：新增 `tools/lib/dsh-runtime.mjs` 可移植探测 DSH 运行时依赖
  （`DSH_RUNTIME_ROOT` → `dsh` 可执行文件位置 → Node 前缀，含 DSH 自身嵌套依赖），
  四个工具里写死的 `C:/Users/-/…` 路径全部消除。
- `verify.satisfied` 在"探测未实现"分支不再是缺失字段，而是**显式 `null`**（三态契约必须完整）。

### 测试

单元测试 **113 → 143 项**（新增 transport 9 + plugin-host 21）；SSH 端到端 **25 → 27 项**
（新增 verify 四情形与取消贯通，均走真实 SSH 协议）。`npm run check` 全绿。

---

## v0.1.0 —— M1「真正能连上并执行」（2026-09-14）

- SSH 传输层（`ssh2`）：密码/密钥认证、单连接复用 + keepalive、主机密钥 TOFU 固定、
  argv 严格转义、exec/stdin 文件投递、超时。
- 参数 → argv 接线机制：**由 argv 用法自动推导**，不支持的分支 fail-closed（无法"谎报已接线"）。
- `probe.facts` 经 **SSH stdin 投递**执行（零安装，远端只要有一个 POSIX sh）+ 画像落盘。
- 免密登录事务（启用 + 撤销）：写 key → **另开连接验证成功** → 才切断旧连接；
  失败回滚且保留旧连接；撤销前先用口令验证"还有路可走"。
- 掩码凭据录入 CLI（交互式不回显、二次确认、原子写，且**从不输出任何凭据值**）。
- 6 个工具、4 个动作、审计哈希链（落盘 + 轮转 + 跨文件校验）、统一脱敏、
  每日报告按天存文件 + 定时调度。

---

## v0.0.1 —— M0「骨架与推演」（2026-09-14）

纯逻辑骨架 + 故障推演：风险分级与审批决策、计划构建与状态指纹（TOCTOU）、
参数校验、审计链、日报子系统、拒密与统一脱敏。
此阶段**不具备真实执行能力**，所有执行路径按 fail-closed 阻断。
